/**
 * @fileoverview Public entry point for the server subpath (`@bymax-one/nest-notification`).
 * @layer api
 *
 * Re-exports the dynamic module, DI tokens, public interfaces, reference
 * providers, the error catalog, and the canonical constants. Populated as the
 * foundation surface lands.
 */

export {}
