/**
 * @fileoverview Public entry point for the React subpath (`@bymax-one/nest-notification/react`).
 * @layer api
 *
 * Browser-targeted hooks (`useOtpInput`, `useOtpCountdown`). Reserved until the
 * frontend surface is implemented.
 */

export {}
